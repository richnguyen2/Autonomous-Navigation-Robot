import csv
import numpy as np
import matplotlib.pyplot as plt
"""
x = [1, 2, 3]
y1 = [2, 4, 6]
y2 = [3, 3, 3] 

with open("control_data.csv", "w", newline="") as file:
    writer = csv.writer(file)

    writer.writerow(["time", "current_response", "desired_response"])

    # Whenever you receive a measurement:
    writer.writerows(zip(x, y1, y2))

plt.plot(x, y1, label = "Current Response")
plt.plot(x, y2, label = "Desired Respones")
plt.legend()
plt.show()
"""
    